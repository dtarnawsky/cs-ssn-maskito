import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.ssn.maskito',
  appName: 'cs-ssn-maskito',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
